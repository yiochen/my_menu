---
status: accepted
---

# Preserve service identity when creating an account

Creating a new MyMenu account links a sign-in method to the guest installation's
existing service identity so its allowance, usage, and active processing remain
continuous. Signing into an existing account switches service identity without
merging guest usage; in both cases, the device-local menu remains untouched.

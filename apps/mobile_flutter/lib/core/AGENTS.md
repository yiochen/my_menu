# Core Layer

Core holds reusable infrastructure that should not depend on feature widgets:

- persistence
- file access
- networking
- logging
- utilities

Keep this layer product-agnostic when possible.


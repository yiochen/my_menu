# Backend

Backend implementation lives here. Keep app-facing contracts in `contracts/`
and avoid mixing backend implementation details into the Flutter app.

Supabase infrastructure belongs under `backend/supabase/`. Run Supabase CLI
commands from `backend/` so the CLI uses `backend/supabase/` as the project
directory.
